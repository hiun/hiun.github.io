---
layout: post
published: false
title: "컴퓨터기술, 특히 프로그래밍 분야와 머신러닝 분야에 대해, 그것의 공통된 정의에 대해."
---

딥러닝 분야를 보면 몇가지 컴포넌트로 이루어져 있다.
그리고 몇가지 관련 개념들이 있다.

gradient-based learning 
학습 loss 의 변화량이 줄어들겠금, 학습하는데에, loss function 의 gradient / slope 를 참조 하는 것이다. slope 의 상황에따라, 
[디테일 하게 확인]
[SGD 의 작동 방법, Adam 의 작동 방법]

partial differentiation : 이건 일종의 calculating the contribution factors 이다.

loss function.
backpropagation.

forward-loss-backward 과정에 있어,
backward 의 피드백을 어느 강도로 모델에 반영 시키나?
그게 sgd 나 adam 의 역할.

==

모델의 모든 컴포넌트에서
partial differentiation 이 계산 됨.

즉 모델의 모든 컴포넌트는,
loss 를 줄이는데 기여해야 하며,
이를 위한 적절한 controllable 한,
case by case 를 만들 수 있는,
높낮이를 만들 수 있는,
양적 질적 표현력을 가질 수 있어야 함.   <- variance 와 bias.

또한 매우 안정적이어야 함.
^ 이 부분에서 
normalization (값을 scaling 하고) - at model component level.
regularization (모델의 variance 를 낮추고) - at loss level.

모델 capacity 는
복잡한 변환을 위해, scaling 을 키워,
복잡한 변환을 위해, 단계적으로 해나가는것.

==

그러한 변환을 sequential 하게 하면 RL 이 되고,
spatial 하게 하면?

==

^ 상기가 ml 의 전부이다.

이러한 변환을 위해,
이러한 변환을 잘 하기 위해,
최적의 모델을 찾으면 된다.
여기서 모델과 accompanying 된 optimization method 를
task 에 따라서 바꿔 끼워 가면 되는 것.

^ 이것이 머신러닝의 전부이다.

(구체화 하기)

==

=============
=============

프로그래밍에 대해:

기본적으로 프로그래밍은 deductive 하다.
어떤게 deduction 인가?
deduction 이라는게 뭔가?

세상을 어떻게 structured 하게 표현 하고,
deductive 하게 처리 하는가?

결국 human level task 를 어떻게 처리 하는 지에 대한 논의.

tree, list, set <- 이런 것들이 어떠한 deductive human level task 를 뜻하는가?

list 는 [A, B, C] 처럼 sequence 가 있는것.
map 은 pair
scalar (float, long) <- 이건 기본적으로 number 이다.

vector <- list
tensor <- list of list

dynamic programming
-> reuse of the pattern extraction
-> reuse of the computed result

greedy
-> approximative pattern extraction and data exploration

알고리즘 -> data structure 에서 **coarse grained pattern recognition** 을 하는 것.
subjective decision making process for given human task.

-> 데이터 / 환경 상에서 가능성을 타진 하는 방법.

기본적으로 패턴 인식이다.
왜냐하면 알고리즘이란
**환경을 탐색하는 방법이기 때문이다.**
sorting, graph (e.g. shortest path)
tree
hnsw

e.g. string manipulation, find number of island, best time to buy and sell stock.
^ case by case 를 탐색 하는 방법이다.


이러한 관점에서,
SVM, DNN, GBDT 등은,
알고리즘의 coarse-granularity 에서 더 나아가서,
fine-grainularity 에서 패턴을 추출 하는 방법으로 볼 수 있다.
